# Released governance inconsistency fixture

This closed schema-2 database was generated by the original v0.6.0 source at
f17b0293597dcdf9fad4fcba9ea19e20d1d91674. The generator executes real correction
and deletion lifecycles and asserts the historical contradiction: ReceiptStatus
reports processing while SubmitEvidence reports failed. No SQL is used to
manufacture the defect. The manifest records hashes, a fixed test clock, and
synthetic authorization and receipt requests. All content is test-only.

Archive that commit into an empty temporary directory. Copy the retained
`generate_test.go.txt` to `internal/appliance/generate_governance_test.go`, then
run from the archived source:

```sh
GOWORK=off RELEASE_FIXTURE_OUTPUT=/absolute/empty/output \
  go test ./internal/appliance -run '^TestGenerateReleasedGovernanceProcessingFixture$' -count=1
```

The current upgrade test verifies automatic repair, preserved receipt text and
identity, matching public statuses, unchanged cancelled jobs, and repeat Open.
